/*
const quotesDefaultState = [];
export default (state = quotesDefaultState, action) => {
  switch (action.type) {
    case 'Add_Quote':
      return [
        ...state,
        state.quote
      ]
  }
} */