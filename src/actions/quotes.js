import { v4 as uuidv4 } from 'uuid';

// Add Quote
export const addQuote = (
  {
    quote='',
    author = '',
  } = {}
)


//demo state
const demo state = {
  quote: [{
    quote: 'be happy',
    author: 'max'
  }]
}